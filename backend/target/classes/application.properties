server.port=8080

# Allow large media uploads (up to 500 MB)
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# Temp directory for video processing (uses system default when blank)
format.adapter.temp-dir=

# Logging
logging.level.com.creativeops=INFO
